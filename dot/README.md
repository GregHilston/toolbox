# Dot

A directory containing all my configuration files.

## What Is In This Directory?

```bash
├── config/nvim/init.vim                # nvim config
├── config/redshift.conf                # redshift config
├── hammerspoon/init.lua                # legacy hammerspoon config (for OSX)
├── i3/config                           # i3 config
├── i3-gaps/                            # i3-gaps config
├── vim/                                # TODO
├── zsh-custom/                         # legacy zsh custom theme
├── .profile                            # TODO
├── gnzh_customized_by_grehg.zsh-theme  # TODO
├── p10k.zsh                            # Our ZSH power10k configuration generated by answering the config questionnaire
├── README.md                           # This document
├── skhdrc                              # skhdc config (for OSX)
├── spectacle_config.png                # legacy config spectacle (for OSX)
├── tmux.conf                           # tmux config
├── vimrc                               # vim config (We prefer to use config/nvim/init.vim, but this is kept around incase nvim isn't available)
├── vs_code_settings_sync_gist_url.txt  # gist for vs code extension settings
├── yabairc                             # yabairc config (for OSX)
└── zshrc                               # zsh config
```
