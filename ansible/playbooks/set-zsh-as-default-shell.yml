- name: ZSH
  hosts: "localhost"
  become: true
  tasks:
    - name: Change user shell to zsh
      become: true
      ansible.builtin.user:
        name: "{{ ansible_user_id }}"
        shell: /bin/zsh
