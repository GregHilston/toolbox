The tests in tests/ fail. Find and fix the bugs in src/ivl/schedule.py so the
whole suite passes. Fix the code, not the tests: do not edit anything under
tests/. You are done when `uv run pytest` passes.
